# Unix

Shell commands, scripting patterns, and reusable "model" scripts.

Organized for quick lookup and copy-paste into real projects.

See:
- `commands/` for categorized references and model one-liners
- `models/` for small, versioned script project structures

Follow the same versioning rules as language models (see root docs/naming-and-versioning.md).