apiVersion: v1
kind: Service
metadata:
  name: my-web
spec:
  selector:
    app: my-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
# For external access consider Ingress or LoadBalancer in later model versions.