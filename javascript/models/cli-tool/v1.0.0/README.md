# JavaScript CLI Tool — Model v1.0.0

Minimal modern CLI tool skeleton (Node + ESM or TypeScript-ready).

## Quick copy
```bash
cp -r .../dev-models/javascript/models/cli-tool/v1.0.0/* ./my-cli/
cd my-cli
npm install
node src/index.js --help
```

## Key files in this version
- package.json (type: module)
- src/index.js (yargs or native commander-style placeholder)
- Basic bin entry

See the files in this directory for the exact recommended layout.